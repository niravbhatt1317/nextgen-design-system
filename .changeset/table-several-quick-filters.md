---
'@mtdt/nextgen-design-system': minor
---

DataTable: `quickFilter` takes one quick filter or several — each gets its own square and menu in the strip, and a row must satisfy every one that has a tick. A filter's `value` may return several strings; the row passes when any of them is ticked (a service account's home organisation and its reach).
