---
'@mtdt/nextgen-design-system': patch
---

Input: the border is neutral-30 (neutral-110 in dark), the same edge as the outline Button and the Toolbar's controls. The Toolbar had been forcing that colour onto any input inside it; an Input anywhere else - a search above a table in a drawer - came out darker (the --mdt-input token, neutral-40).
