---
'@mtdt/nextgen-design-system': patch
---

IconTile: the tile sizes its own glyph — 14 in the 24px tile, 16 in 32, 20 in 40, 24 in 48. A page no longer has to know the ratio, and an icon that arrives at its default 20px no longer overflows the small tile.
